# Folded fighter sprite

Asset: `Content.CMU/Resources/Textures/CMU14/Vehicles/Fighter/jetfighter.rsi/folded.png`.

Created with the built-in imagegen tool from the user-supplied `vtolmode.png`, then imported at the RSI's existing 266 × 335 resolution with nearest-neighbor sampling and its alpha preserved. Original RSI attribution is unchanged. The folded state is used while grounded; the original VTOL state is used during takeoff and landing. Pylons fold inward with the wings.

Prompt:

> precise-object-edit: Edit this existing pixel-art top-down fighter sprite to make its naval parked version with both MAIN WINGS FOLDED UP. Final game asset on a genuinely transparent RGBA background. Preserve exactly the original center fuselage, nose pointing DOWN, two open tandem cockpit sockets, engines, tail fins, palette, dark outlines, pixel art resolution and alignment. Do not add crew, text, shadows on a floor, scenery, or new weapons. Only change the broad main wings on the left and right: hinge each outer wing upward, seen from straight above as narrow edge-on wing panels closely alongside the engine/fuselage rather than stretching sideways. Mechanical hinge strips with subtle light edges, clearly believable folded wings. Leave the original tail fins unmodified. The aircraft stays exactly the same length and centerline and y alignment as the input. Keep the original transparent canvas 266 pixels wide by 335 pixels tall if possible; if a larger output is necessary preserve exactly that aspect ratio and original sprite registration, do not crop tight or recenter the now narrower silhouette. It must fit as a sibling RSI sprite state so only the wings change between states. Pixel art only, no soft painting, no antialiasing, no glossy illustration.
